const countriesController = require('./countries'),
      citiesController = require('./cities'),
      photosController = require('./photos');

module.exports = {
  countriesController,
  citiesController,
  photosController
};